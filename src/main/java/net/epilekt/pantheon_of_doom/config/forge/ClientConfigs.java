package net.epilekt.pantheon_of_doom.config.forge;

import net.epilekt.pantheon_of_doom.client.PlayerHp;
import net.minecraftforge.common.ForgeConfigSpec;

public class ClientConfigs {

    //public static final ForgeConfigSpec.Builder BUILDER = new ForgeConfigSpec.Builder();
    //public static final ForgeConfigSpec.ConfigValue<PlayerHp> HEALTH_BAR_DISPLAY;
    //public static final ForgeConfigSpec.ConfigValue<PlayerHp> HEALTH_BAR_ANCHOR;
    //public static final ForgeConfigSpec SPEC;

    static {
        //BUILDER.push("Animations");
        //BUILDER.comment("What to render in first person while alive.");
        //BUILDER.pop();
//
        //BUILDER.push("UI");
        //HEALTH_BAR_DISPLAY = BUILDER.defineEnum("healthBarDisplay", PlayerHp.HealthBarOverlay.Display.Always);
        //BUILDER.comment("Use to adjust if the mana bar conflicts with other mod's ui elements (11 is one full hunger bar up).");
       //HEALTH_BAR_TEXT_VISIBLE = BUILDER.define("healthBarTextVisible", true);
        //HEALTH_BAR_ANCHOR = BUILDER.defineEnum("healthBarAnchor", PlayerHp.HealthBarOverlay.Anchor.XP);
        //BUILDER.pop();
//
        //BUILDER.push("Renderers");
        //BUILDER.pop();
//
        //SPEC = BUILDER.build();
    }
}